
    // =============================================================

    receivee {
        revert("

    fallback() external pa

    // ========================================
    // VERSI===================================

    function version() external pure returns (string memory) {
        return "3.0.0";
    }
}
      
